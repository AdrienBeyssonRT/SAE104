{% extends 'base.html.twig' %}

{% block title %}Compétences{% endblock %}

{% block body %}
    <h2>Mes Compétences</h2>

    <div class="container">
        <div class="mb-4">
            <h4>Langages de Programmation</h4>
            <div class="progress">
                <div class="progress-bar" role="progressbar" style="width: {{ random(30, 100) }}%;" aria-valuenow="{{ random(30, 100) }}" aria-valuemin="0" aria-valuemax="100">{{ random(30, 100) }}%</div>
            </div>
        </div>
        
        <div class="mb-4">
            <h4>Web Development</h4>
            <div class="progress">
                <div class="progress-bar" role="progressbar" style="width: {{ random(30, 100) }}%;" aria-valuenow="{{ random(30, 100) }}" aria-valuemin="0" aria-valuemax="100">{{ random(30, 100) }}%</div>
            </div>
        </div>
        
        <div class="mb-4">
            <h4>Frameworks</h4>
            <div class="progress">
                <div class="progress-bar" role="progressbar" style="width: {{ random(30, 100) }}%;" aria-valuenow="{{ random(30, 100) }}" aria-valuemin="0" aria-valuemax="100">{{ random(30, 100) }}%</div>
            </div>
        </div>
        
        <div class="mb-4">
            <h4>Base de données</h4>
            <div class="progress">
                <div class="progress-bar" role="progressbar" style="width: {{ random(30, 100) }}%;" aria-valuenow="{{ random(30, 100) }}" aria-valuemin="0" aria-valuemax="100">{{ random(30, 100) }}%</div>
            </div>
        </div>
        
        <!-- Ajoutez d'autres compétences au besoin -->
    </div>
{% endblock %}
